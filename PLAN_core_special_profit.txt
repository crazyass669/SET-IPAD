========================================================================
 แผนงาน: แยก "กำไรปกติ" / "กำไรพิเศษ" ในเมนู 🚀 หุ้นโตแรงรายไตรมาส
 สร้าง: 2026-09-04
 แหล่งข้อมูลที่เลือก: SET P&L รายไตรมาส (financialstatement API, account code)
 สถานะ: 🔶 เสร็จสมบูรณ์ทั้ง 4 phase (Phase 0-4) — ใช้งานได้จริงแล้ว รอผู้ใช้ยืนยันสถานะ
        coverage จริง Q2/2569 = 317/911 หุ้น (35%) — ต่ำกว่าที่ประเมินไว้ (~45-50%)
        เพราะ sample ตอนวางแผนเน้นหุ้นใหญ่ ตลาดเต็มมีหุ้นเล็กไม่มี FX/อนุพันธ์เยอะกว่า
========================================================================

------------------------------------------------------------------------
 ที่มา / โจทย์
------------------------------------------------------------------------
เมนู "หุ้นโตแรงรายไตรมาส" โชว์ กำไรสุทธิ + QoQ/YoY แต่กำไรสุทธิรวม
"รายการครั้งเดียว" (FX, mark-to-market อนุพันธ์, ด้อยค่า) ทำให้ QoQ/YoY
หลอก — หุ้นดู "โตแรง" ทั้งที่ธุรกิจจริงไม่ได้โต (หรือกลับกัน)

ต้องการ: แยกกำไรสุทธิเป็น
  - กำไรปกติ  = กำไรจากธุรกิจหลัก (recurring)
  - กำไรพิเศษ = รายการไม่เกิดประจำ (non-recurring)

เทียบ 4 แหล่งแล้ว (2026-09-04):
  SET P&L รายไตรมาส  — มี account code 439700 "กำไร(ขาดทุน)อื่น" แยกชัด
                        มาตรฐาน SET ตรงกันทุกบริษัท  ← เลือกอันนี้
  Yahoo yahoo_q      — มี Normalized Income สำเร็จรูป แต่ช้า 1 ไตรมาส +
                        auto-detect เพี้ยนกับหุ้นไทยบางตัว
  SET company-highlight (set) — profitFromOtherActivity เป็นรายปี/6M เท่านั้น
  Finnomena finnomena_q — มี income แค่ 5 บรรทัด ไม่มีทางแยก

ผู้ใช้ยืนยัน: ปกติดูแค่ไตรมาสล่าสุด → ข้อจำกัดเรื่อง "ลึกแค่ ~4 ไตรมาส"
ของ SET ไม่กระทบ use case จริง

------------------------------------------------------------------------
 ⚠️ 4 ข้อจำกัดที่รับรู้แล้ว (NOTES — อ่านก่อนแก้/ก่อนเชื่อตัวเลข)
------------------------------------------------------------------------

[1] ธนาคาร / เงินทุน / ประกัน จะได้ "—" (ไม่มีตัวเลข)
    ผังบัญชีกลุ่มการเงินคนละแบบ — ไม่มี 439700, ไม่มีบรรทัด EBIT (409992)
    เช็คแล้ว: KBANK/KTB/TISCO/BLA ไม่มีทั้งคู่
    กลุ่มนี้ ~15% ของตลาดโดยจำนวน แต่รวมชื่อใหญ่ (KBANK/SCB/BBL/KTB/
    ประกันชีวิต ฯลฯ) — feature นี้ใช้กับกลุ่มการเงินไม่ได้ ต้องโชว์ "n/a"
    ไม่ใช่ 0

[2] 439700 ครอบ ~90% ของ "รายการพิเศษ" ไม่ใช่ 100%
    จับครบ: FX (439710) + อนุพันธ์ FVTPL (439720) + hedge (439770)
            = ก้อนใหญ่สุดและผันผวนสุด
    อาจตกหล่น: กำไรขายสินทรัพย์ / ด้อยค่า goodwill / ค่าเผื่อการด้อยค่า
            ที่บางบริษัทลงใน 410200 "รายได้อื่น" หรือ 429800 "ค่าใช้จ่ายอื่น"
            แทน — ตัวเหล่านี้จะไม่ถูกจับ
    ตัดสินใจ: ไม่รวม 410200/429800 เข้าสูตร (เสี่ยง over-strip — สอง
             บรรทัดนี้ปน recurring income เยอะ) ยอมพลาด one-off ที่ซ่อน
             อยู่ในนั้น ดีกว่าตัดรายได้จริงทิ้ง
    → ป้าย badge ต้องสื่อว่า "จับเท่าที่ SET แยกบรรทัดให้" ไม่ใช่
      "กำไรปกติสมบูรณ์แบบ"

[3] ~40% ของหุ้นนอกกลุ่มการเงิน ไม่แยกบรรทัด 439700 เลย
    เช็ค Phase 0 (สุ่ม 17 ตัวนอกกลุ่มการเงิน): 7 ตัวไม่มี 439700 —
    SCC / OSP / KCE / COM7 / BJC / CRC / CBG (Q2/2026)
    ค้าปลีก/consumer หลายตัวไม่มี FX/อนุพันธ์จริง → "ไม่มีรายการพิเศษ" ถูกต้อง
    แต่ SCC/OSP น่าจะมี (net เข้าบรรทัดอื่น / ค่าเล็ก SET ตัดแถวทิ้ง)
    ผลลัพธ์: feature นี้ "core = net, special = 0" สำหรับหุ้นกลุ่มนี้ —
    ไม่ได้เพิ่มข้อมูลอะไร และอาจพลาด one-off ที่ซ่อนอยู่
    → เมื่อ special = 0 เพราะ "ไม่มีบรรทัด" ต้องต่างจาก special = 0 เพราะ
      "มีบรรทัดแต่ค่า = 0" ในเชิง UI (badge/tooltip บอกว่า SET ไม่แยกให้)
    → รวม [1]+[3]: feature ให้ค่าจริงกับหุ้นราว ๆ 45-50% ของตลาด
      (นอกกลุ่มการเงิน ที่แยก 439700) เท่านั้น

[4] ต้องแก้ pipeline — ตอนนี้ทิ้ง account code ทั้งหมด
    _set_qpl_row_from_amt() ปัจจุบันเก็บแค่ 10 field (revenue/cogs/gross/
    sga/op/fin_cost/pretax/tax/net) — โยน 439700/4397xx/431xxx ทิ้งหมด
    ต้องเพิ่มให้เก็บ + carry ผ่าน merge + bake
    ผลข้างเคียง: ไตรมาสเก่าที่ sync เก็บไว้แล้วจะไม่มี field ใหม่ (SET
    periods endpoint refetch ย้อนหลังไม่ได้) — มีเฉพาะ ~4 ไตรมาสที่ยัง
    อยู่ใน periods list ตอน sync รอบแรกหลัง deploy เป็นต้นไป

------------------------------------------------------------------------
 สูตร / นิยาม
------------------------------------------------------------------------
account code (SET taxonomy — ยืนยันแล้วว่า parent = ผลรวม children):
  439700  กำไร(ขาดทุน)อื่น           ← parent total ใช้ตัวนี้ตรง ๆ
    439710  จากอัตราแลกเปลี่ยน (FX)
    439720  จากเครื่องมือการเงิน FVTPL (อนุพันธ์ mark-to-market)
    439770  จากการบัญชีป้องกันความเสี่ยง (hedge)
    439790  อื่น ๆ
  431000  (กลับรายการ)ขาดทุนลดมูลค่าสินค้าคงเหลือ  ← option: รวมด้วย
  431100  ส่วนแบ่งกำไร(ขาดทุน)บริษัทร่วม (equity method) ← ไม่รวม (recurring)

หน่วย: SET รายงานเป็น "พันบาท" → ×1000 (เหมือน field อื่นใน _set_qpl_amt_map)
หมายเหตุตำแหน่ง: 439700 อยู่ "เหนือ" บรรทัด EBIT 409992 ในงบ SET
  → operating_profit / net_profit ปัจจุบันรวม 439700 อยู่แล้ว
  → "กำไรปกติ" = ถอด 439700 ออก

  special_pretax     = amt(439700)              (+ amt(431000) ถ้าเปิด option)
  etr                = tax_expense / pretax_profit   guard → clamp [0, 0.35],
                       fallback 0.20 ถ้าหารไม่ได้
  special_after_tax  = special_pretax * (1 - etr)
  core_profit        = net_profit - special_after_tax
  special_share_pct  = |special_after_tax| / |net_profit| * 100
  core_profit_yoy    = pct_change(core_profit, core_profit ของ Q เดียวกันปีก่อน)
                       — None ถ้าปีก่อนไม่มี 439700 หรือฐาน <= 0

badge ⚡ "กำไรพิเศษ": โชว์เมื่อ special_share_pct >= 20 AND
                      |net_profit| >= 50 ลบ. (GROWTH_SCR_MIN_BASE กันฐานจิ๋ว)

edge:
  - net_profit = "ผู้ถือหุ้นบริษัทใหญ่" (ไม่รวม NCI) แต่ 439700 เป็นก่อนแบ่ง
    NCI → มี error เล็กน้อยในหุ้นที่ NCI เยอะ ยอมรับได้
  - ไม่มี 439700 ในงบ → special = None (กลุ่มการเงิน) หรือ 0 (นอกกลุ่ม
    การเงินที่ไม่แยกบรรทัด) — สองเคสนี้ต่างกัน ดูข้อ [1]/[3]

------------------------------------------------------------------------
 ไฟล์ที่ต้องแตะ
------------------------------------------------------------------------
BACKEND — sources/financials_store.py
  A. _set_qpl_amt_map()          + เก็บ key แบบ "#<accountCode>" คู่กับ key
                                   ชื่อบัญชีเดิม (namespaced กันชนกัน) เพื่อให้
                                   builder อ้าง code ได้ (ชื่อบัญชีสะกดไม่นิ่ง)
  B. _set_qpl_row_from_amt()     + other_gl_pretax = g("#439700")
                                   + fx_gl / deriv_gl / hedge_gl (เผื่อโชว์ย่อย)
                                   + inv_writedown = g("#431000")
                                   ใส่เข้า row ที่ return
     * sub() ใน _fetch_set_cumulative_series ทำงานกับทุก key อยู่แล้ว
       (set(a)|set(b)) — "#439700" ลบงวดสะสมได้เอง ไม่ต้องแก้
  C. _merge_set_qpl_payload()    — เช็คว่า field-level merge รับ key ใหม่
                                   (น่าจะได้อยู่แล้ว — merge ทั้ง dict)
  D. _qpl_stock_growth_rows()    + pass-through: special_pretax (cur),
                                   special_pretax_prior (prior key), tax/pretax
                                   สำหรับคำนวณ etr
  E. get_qpl_growth_screener()   + คำนวณ core_profit / special_after_tax /
                                   core_profit_yoy / special_share_pct ต่อ row
                                   (จุดเดียวกับที่แนบ ocf/ps อยู่แล้ว)
                                   + param include_core=True (ปิดได้เผื่อ caller อื่น)
  F. (option) compute_qpl_report() — SET layer ก็ผ่าน _set_qpl_row_from_amt
                                   → เพิ่ม row["other_gl"] ให้ป๊อปอัพ
                                   "🧾 งบการเงิน" โชว์ด้วย (ของแถมงานน้อย)

BACKEND — app.py
  G. /api/quarterly-growth-screener — ไม่ต้องแก้ logic (get_qpl_growth_screener
     คำนวณให้) แค่เช็คว่า _qpl_parsed_cache invalidate ตอน sync (มีอยู่แล้ว
     L5054 _qpl_parsed_cache.clear())

FRONTEND — static/dashboard.js  (block QUARTERLY GROWTH SCREENER ~5268+)
  รูปแบบ UI ที่ล็อกแล้ว (2026-09-04, ทดสอบใน mockup จนพอใจ):
    − ตัดคอลัมน์ 'Sector' ออก (ปุ่มกรอง sector dropdown ยังอยู่)
    − ตัดชื่อบริษัทใต้ชื่อย่อออกจากคอลัมน์ 'หุ้น' (ย้ายไป title tooltip แทน
      — ชื่อยาวไม่จำกัดความกว้างเคยดันคอลัมน์นี้กว้าง ~150-220px)
    + 3 คอลัมน์ใหม่ถัดจาก 'กำไรสุทธิ': 'กำไรพิเศษ' (จำนวนเงิน) →
      'กำไรปกติ' (จำนวนเงิน) → 'พิเศษ %' (ของกำไรสุทธิ)
    → รวม 15 คอลัมน์ (วันนี้ 13) แต่ความกว้างจริง ~1,190px ยังแคบกว่าวันนี้
      (~1,250px) เพราะตัด Sector+ชื่อบริษัทออกมากกว่าที่เพิ่มเข้า
    ไม่ทำ 'กำไรปกติ YoY' (ข้อจำกัด #4 — คำนวณไม่ได้ ~1 ปี)
  H. GROWTH_SCR_COLS  + 3 รายการถัดจาก net_profit:
       { key:'special_after_tax', label:'กำไรพิเศษ' }
       { key:'core_profit',       label:'กำไรปกติ' }
       { key:'special_share_pct', label:'พิเศษ %' }
  I. _growthScrCols()  — เดิม filter 'sector' ออกให้ dr/us/hk
       (_GROWTH_SCR_HAS_SECTOR) → ตัด 'sector' ออกจาก GROWTH_SCR_COLS ทุก
       universe ไปเลย (ไม่ใช่ filter ต่อ uni อีกต่อไป) · 3 คอลัมน์ใหม่โชว์
       เฉพาะ th (_GROWTH_SCR_CORE = Set(['th']))
  I2. case 'symbol' ใน _growthScrCellHtml() — ลบ `<div class="text2">${name}</div>`
       บรรทัดชื่อบริษัท → ย้าย name (+ sector) ไปเป็น title="" บน <a class="sym-link">
       แทน (hover เห็น)
  J. _growthScrCellHtml() เพิ่ม 3 case:
       'special_after_tax' → _growthScrThb (มี +/− nam), หุ้นการเงิน/ไม่มีบรรทัด → "—"/"—﹡"
       'core_profit'       → _growthScrThb, ไม่มีบรรทัด → net + ﹡ (สี dim)
       'special_share_pct' → เลข% ธรรมดา, **แดง+ตัวหนา+⚡ เมื่อ >= 20**,
                              เทาจาง เมื่อต่ำกว่า, "—"/"—﹡" เหมือนสองคอลัมน์บน
       ทุก case: หุ้นการเงิน (is_fin) → "—" ล้วน + tag "กลุ่มการเงิน" (เฉพาะ
       คอลัมน์กำไรพิเศษพอ ไม่ต้องซ้ำทุกคอลัมน์) · ไม่มีบรรทัด 439700 (no_other_line)
       → "—﹡"/net+﹡ ตามแต่ละคอลัมน์ (ดู mockup expand-row "คิดจากอะไร" เป็น
       ต้นแบบ tooltip)
  K. _growthScrSortVal() (~L5572, เทียบ pattern เดิม) — เพิ่ม guard: ถ้า sort
       key เป็น 'special_after_tax'/'special_share_pct' และแถวเป็น is_fin/
       no_other_line → คืน null (ให้ sort ตกไปท้ายเสมอ เหมือน pattern
       Infinity ที่มีอยู่แล้วสำหรับ turnaround)
  L. _growthScrMaskedRows()     — ไม่ต้องแก้ (ไม่มีคอลัมน์ %/YoY แบบเดิมให้ mask)
  M. อัปเดต #growth-scr-th-desc สรุปข้อจำกัด
     ("กำไรปกติ = ตัดบัญชี 'กำไร(ขาดทุน)อื่น' (FX/อนุพันธ์/hedge) ·
       ธนาคาร-เงินทุน-ประกันไม่มีข้อมูล · ~40% หุ้นอื่นไม่แยกบรรทัด = กำไรปกติเท่ากำไรสุทธิ")
  N. bump ?v= ของ dashboard.js ใน set_dashboard.html
     ([[cache-busting-version-bump]])
  * mockup (ล็อกดีไซน์นี้แล้ว): claude.ai/code/artifact/a95cdc46-c736-494a-bd46-3fcda34ba6c8
    ("2 คอลัมน์"/"ย่ออย่างเดียว" toggle ในนั้น = mockup UI, ของจริงคือ
    3 คอลัมน์ + ย่ออย่างเดียว ล็อกตายตัว ไม่มี toggle)

ไม่แตะ:
  - mobile/  (เมนูนี้ซ่อนบนมือถืออยู่แล้ว — [[which-codebase-local-vs-mobile]])
  - financials.db มือ / ไตรมาสเก่าที่เก็บไว้แล้ว
  - set_qpl_chart_series (company-highlight 5 ปี — ไม่มี account code แยก)

------------------------------------------------------------------------
 ลำดับงาน (phase)
------------------------------------------------------------------------
Phase 0 — verify สมมติฐาน  (บางส่วนทำแล้ว 2026-09-04)
  [x] 439700 == ผลรวม 4397xx children — สุ่ม 24 ตัว: 0 mismatch ✓
      ใช้ 439700 ตรง ๆ ได้
  [x] กลุ่มการเงิน (แบงก์/เงินทุน/ประกัน) ไม่มี 439700 — สุ่ม 10 ตัว: 0 มี ✓
  [x] หุ้นนอกกลุ่มการเงินที่ไม่มี 439700 — 7/17 (~40%, สูงกว่าเดาไว้มาก)
  [ ] ชื่อบัญชี 439700 สะกดคงที่ข้ามงวดไหม (Phase 1 จะ key ด้วย code อยู่แล้ว
      — เช็คเสริมว่า accountCode นิ่งข้ามปี)
  [ ] เทียบ core_profit ที่คำนวณ กับบทวิเคราะห์โบรก 3-4 ตัว (sanity)
  [ ] ตัดสิน option 431000 (inventory write-down) — รวมหรือไม่

Phase 1 — backend pipeline  ✅ เสร็จ 2026-09-04
  [x] _set_qpl_amt_map เก็บคีย์ "#<accountCode>" คู่กับชื่อบัญชี (ไม่ทับของเดิม)
  [x] _set_qpl_row_from_amt เพิ่ม other_gl = amt.get("#439700") ในแถวที่คืน
  [x] _qpl_core_special(other_gl, net_profit, tax_expense, pretax_profit) —
      helper ใหม่ คำนวณ (special_after_tax, core_profit, special_share_pct)
      ETR = tax/pretax clamp [0, 0.35] fallback 0.20
  [x] _qpl_stock_growth_rows ส่ง other_gl/tax_expense/pretax_profit ผ่าน
      (get_sector_qpl_compare ไม่กระทบ — whitelist field ก่อน export อยู่แล้ว)
  [x] get_qpl_growth_screener เรียก _qpl_core_special ต่อแถว, pop ฟิลด์ดิบ
      (other_gl/tax_expense/pretax_profit) ไม่ export ออกไปดิบๆ
  [x] validate สด: ยิง SET API จริง PTT/TASCO/SCC/KBANK ผ่าน _set_qpl_amt_map→
      _set_qpl_row_from_amt→_qpl_core_special ครบสาย — PTT other_gl=+11,220 ล.
      core=44,330 ล. (15.6%), SCC other_gl=None (ไม่มีบรรทัด), KBANK row=None
      (ธนาคาร ไม่มี revenue account) ตรงตามคาด
  [x] python -m py_compile ผ่าน

Phase 2 — frontend  ✅ เสร็จ 2026-09-04
  [x] GROWTH_SCR_COLS +3 คอลัมน์ (special_after_tax/core_profit/special_share_pct,
      core:true) · _growthScrCols() กรอง core:true ตาม _GROWTH_SCR_HAS_CORE={th}
  [x] _GROWTH_SCR_HAS_SECTOR เหลือแค่ {jp} (เดิม {th,jp}) — แยกออกจาก
      _GROWTH_SCR_SECTOR_DATA={th,jp} ใหม่ (คุมตัวกรอง dropdown ไม่ให้หายไปด้วย
      ตอนตัด sector คอลัมน์ของ th — บั๊กที่เกือบเกิดถ้าไม่แยก 2 constant นี้)
  [x] case 'symbol': th ตัดชื่อบริษัทออกจากใต้ชื่อย่อ ย้ายเป็น title="" (name·sector)
      universe อื่นไม่กระทบ (ยังโชว์ชื่อใต้ชื่อย่อเหมือนเดิม)
  [x] _growthScrSpecialCell/_growthScrCoreCell/_growthScrShareCell — แยก 2 เคส
      null: กลุ่มการเงิน (badge badge-blue "กลุ่มการเงิน") vs ไม่มีบรรทัด (﹡, core
      fallback = net_profit)
  [x] _growthScrSortVal: core_profit ใช้ net_profit แทนตอน null+ไม่ใช่กลุ่มการเงิน
      (sort แข่งกับหุ้นที่มีข้อมูลจริงได้) special_after_tax/share_pct ไม่ fallback
  [x] _growthScrMaskedRows: mask special_share_pct เมื่อ |net_profit| < 50 ลบ.
  [x] #growth-scr-th-desc เพิ่มย่อหน้าอธิบาย 3 คอลัมน์ใหม่ + ข้อจำกัด
  [x] ?v=20260904h → 20260904i

Phase 3 — ทดสอบ  ✅ เสร็จ 2026-09-04
  [x] node --check dashboard.js ผ่าน
  [x] test server พอร์ต 5011 ([[server-kill-restart-pattern]]) — curl
      /api/quarterly-growth-screener: 910 แถว, field ใหม่มาครบ (null หมด — ยังไม่
      sync ตาม Phase 4)
  [x] Playwright headless: คอลัมน์ th ถูก (14 คอลัมน์ ไม่มี Sector มีกำไรพิเศษ/
      กำไรปกติ/พิเศษ%) · sector filter dropdown ยังอยู่ (36 ตัวเลือก) ·
      symbol tooltip โชว์ name+sector ถูก · sort พิเศษ% ไม่ crash (910 แถว) ·
      US: ไม่มี core cols, ไม่มี Sector (เหมือนเดิม) · JP: มี Sector ไม่มี core cols
      · KBANK row: "—"+badge กลุ่มการเงิน ถูก · PTTGC row: "—﹡"/"...﹡" (SET ไม่แยก
      บรรทัดงวดนี้) ถูก · 0 console/page error ตลอด
  [x] kill server by PID, netstat ยืนยันพอร์ตว่าง
  [ ] spot-check เลขจริงเทียบคำนวณมือ — รอ Phase 4 (ตอนนี้ทุกแถว null เพราะ
      DB ยังไม่มี field other_gl เลย ยังเทียบไม่ได้จนกว่าจะ sync)

Phase 4 — เก็บข้อมูลจริง
  [x] ผู้ใช้กด "🔄 อัพเดทงบการเงินทั้งหมด" 2026-09-04 — แต่พบว่า **ไม่ backfill other_gl
      ให้หุ้นที่ sync ไปแล้วก่อนหน้า** เพราะปุ่มนี้ใช้ skip_up_to_date=True
      (_run_financials_update_all) ซึ่งเช็คแค่ "มี key ไตรมาสล่าสุดอยู่ใน DB หรือยัง"
      (_payload_latest_period) ไม่เช็คว่า field ในแถวนั้นครบ — หุ้นที่มี Q2/2026 อยู่แล้ว
      (เกือบทั้งตลาด) เลยไม่ถูกดึงซ้ำ, ตรวจ DB จริงพบ other_gl เข้าแค่ 7/1787 symbol
      (เฉพาะตัวที่ยังไม่ up-to-date ด้วยเหตุผลอื่น)
  [x] แก้ปัญหา: sync_set_qpl_series() ต่อ symbol ตรง ๆ (ไม่ผ่าน sync_all wrapper —
      ไม่มี skip_up_to_date gate) ทดสอบ 15 ตัว (PTT/TASCO/SCC/KBANK/AWC/BANPU/ฯลฯ)
      → เขียนจริงลง financials.db สำเร็จ (verify อ่านกลับจาก DB ตรง) เร็ว ~0.5s/หุ้น
      (ไม่ใช่ ~1 ชม. อย่างที่ comment เดิมกลัวไว้ — comment นั้นพูดถึง full 9-source sync
      ไม่ใช่ set_qpl เดี่ยว ๆ)
  [x] รัน backfill เต็ม 927 หุ้นไทย (data/set_data.json market SET/mai) background —
      สคริปต์ scratchpad/backfill_other_gl.py — เสร็จจริง 927 ok, 0 fail, 8.1 นาที
  [x] coverage งวด Q2/2569: 317/911 หุ้นที่มี net_profit (35%) คำนวณกำไรปกติได้จริง ·
      58 ตัวกลุ่มการเงิน (—) · 536 ตัว SET ไม่แยกบรรทัด 439700 งวดนี้ (—﹡)
      (ต่ำกว่า ~45-50% ที่ประเมินไว้จาก sample หุ้นใหญ่ 66 ตัวตอนวางแผน — ตลาดเต็มรวม
      หุ้นเล็กที่ไม่มี FX/อนุพันธ์เยอะกว่า)
  [x] spot-check ผ่าน browser จริง (test server 5011, kill แล้ว): เรียง "พิเศษ %" desc
      ได้ BANPU ⚡124% กำไรสุทธิ 1,778.7 ลบ. → กำไรพิเศษ +2,208.5 ลบ. → **กำไรปกติ
      -429.8 ลบ. (พลิกเป็นขาดทุน!)** ตรงกับที่พบตอน explore ครั้งแรก · ASP ⚡104% ·
      ITD ⚡99% · ทุกค่าตรงกับตัวเลขที่ตรวจสอบสดตอน Phase 1 (PTT/TASCO/AWC ฯลฯ)
      0 console error

**Phase 4 เสร็จสมบูรณ์ — ฟีเจอร์ใช้งานได้จริงกับข้อมูลจริงแล้ว 2026-09-04**

Phase 5 — option ต่อยอด (ไว้ทีหลัง)
  [ ] ป๊อปอัพ "🧾 งบการเงิน" โชว์ "กำไร(ขาดทุน)อื่น" จาก SET layer (F)
  [ ] แท็บ DR: fallback Yahoo Total Unusual Items (คนละสูตร — ต้องคิด
      เรื่อง label ว่ามาคนละแหล่ง)

------------------------------------------------------------------------
 ประเมินงาน
------------------------------------------------------------------------
Phase 0-3: ~1 วันทำงาน (6-8 ชม.)   Phase 4: ผู้ใช้กด + รอ sync
เสี่ยงต่ำ — เพิ่ม field ไม่แตะ logic เดิม, universe อื่นไม่กระทบ,
ไตรมาสเก่าแค่ไม่มีข้อมูล (ไม่ error)

related memory: [[growth-screener-code-review-22C-2026-09-04]]
               [[growth-screener-foreign-2026-08-30]]
               [[qpl-quarterly-report-view]]  [[financials-menu-code-review-B3-2026-09-03]]
========================================================================
