========================================================================
 CALC_RISK_AUDIT รอบ 4 — เป้าหมาย 5 จุดที่ผู้ใช้เลือกเองหลังคำถาม "มีอะไรแนะนำเพิ่มไหม"
 ต่อจาก ROUND3 (Cash Cycle/Peer Group Detail/Sankey Sector — ปิดครบ 3/3 แล้ว, เจอบั๊กจริง
 2 ใน 3 ข้อ) — ต่างจาก ROUND1-3 ตรงที่ระหว่างร่างไฟล์นี้ได้ query ข้อมูลจริง/อ่านโค้ดคร่าวๆ
 ของทั้ง 5 ข้อไปแล้วก่อนสร้างไฟล์ (ไม่ใช่แค่เดาความเสี่ยงจากชื่อเมนู) พบว่าความเสี่ยงจริง
 ต่างกันมากระหว่างข้อ — ไม่ใช่ทุกข้อมีบั๊กเท่ากัน
 สร้าง: 2026-09-06

 ⚠ ข้อสังเกตสำคัญก่อนใช้ไฟล์นี้: จาก 5 ข้อที่ผู้ใช้เลือก มีแค่ [1] Payout Ratio ที่ยืนยันแล้ว
 ว่ามีบั๊กจริงชัดเจน (query ข้อมูลจริงแล้ว) ส่วน [2]-[5] อ่านโค้ดคร่าวๆ แล้วพบว่ามี guard
 ป้องกัน pattern นี้อยู่แล้วพอสมควร (บางจุดมี comment อ้างอิงถึงบั๊กที่เคยแก้ไปแล้วชัดเจน)
 — เรียงลำดับความสำคัญจากผลตรวจคร่าวๆ นี้ ไม่ใช่เรียงตามที่ผู้ใช้พิมพ์มา


========================================================================
 ขั้นตอนหลังรีวิวแต่ละข้อ (เหมือน ROUND2/3 ทุกอย่าง)
========================================================================
  1. แก้บั๊กที่เจอ (ถ้ามี)
  2. Restart server (ถ้าแก้ app.py/sources/*.py) + bump ?v= (ถ้าแก้ frontend)
  3. เปิดเบราว์เซอร์จริง/ยิง API จริงเช็คผลก่อน/หลังแก้ ไม่ใช่แค่อ่านโค้ด
  4. เขียนบันทึกผลต่อท้ายข้อนั้นในไฟล์นี้เสมอ (แบบ ">>> ตรวจแล้ว/แก้แล้ว [วันที่]")
  5. **ห้ามแก้ [สถานะ] ที่หัวข้อเอง** (⬜→🔶→✅) — ให้ผู้ใช้ติ๊กเองเสมอตามกติกาเดิมทุกไฟล์
     ในชุดนี้
========================================================================


########################################################################
[1] เมนู: 🧾 งบการเงิน / DR / มือถือ — Payout Ratio (DPS÷EPS) ในการ์ดปันผล
สถานะ: ✅
ความเสี่ยงประเมิน: สูง (ยืนยันแล้วด้วยข้อมูลจริงระหว่างร่างไฟล์นี้ — ไม่ใช่แค่เดา)
########################################################################

/code-review medium sources/dividend_stats.py sources/factor_snapshot.py

ไฟล์/บรรทัด:
  - sources/dividend_stats.py:37-54 (compute_payout_ratio)
  - sources/factor_snapshot.py:265-279 (_latest_eps — ที่มาของ eps_value/eps_date)
  - app.py:4301-4324 (/api/dividends ผูก payout_ratio_pct), :6893 (จุดเรียกซ้ำอีกจุด)

ยืนยันแล้วระหว่างร่างไฟล์นี้ (สแกนหุ้นไทยทั้งตลาดจริงผ่าน factor_snapshot.eps_latest +
dividends table ในโค้ดจริง ไม่ใช่ synthetic): compute_payout_ratio guard แค่
`eps_value <= 0` (กันขาดทุน/ศูนย์) แต่ไม่กัน EPS ที่ "บวกจริงแต่จิ๋วผิดปกติเทียบกับปีอื่น
ของบริษัทเอง" — เจอ 24/548 หุ้นที่คำนวณได้ (4.4%) payout ratio เกิน 200% เรียงจากสุดขั้ว:
  AIE 1562.5% (eps=0.016), NAT 1300.0% (eps=0.01), CHASE 1041.0% (eps=0.001),
  CEYE 750.0% (eps=0.01), SPCG 722.2% (eps=0.36), K 550.0% (eps=0.01),
  INSURE 537.6% (eps=1.86), HENG 525.0% (eps=0.01), LEO/LPN/SMD100 500.0%
ยืนยันย้อนดู EPS หลายปีของแต่ละตัวจริงแล้วว่าเป็น "ปีนี้ทรุดฮวบเทียบปีก่อนๆ" ไม่ใช่ธุรกิจ
EPS ต่ำถาวรปกติ — pattern เดียวกับ COGS ของ compute_cash_cycle เป๊ะ (ยืนยัน ROUND3 [1]):
  CHASE: 0.136 → 0.069 → 0.042 → **0.001** (ปีล่าสุด)
  NAT:   0.326 → 0.357 → 0.53  → **0.01**
  HENG:  0.12  → 0.11  → 0.02  → **0.01**
  K:     0.005 → 0.03  → 0.11  → **0.01**
  SPCG:  2.2   → 1.74  → 0.65  → **0.36** (declining ต่อเนื่อง ไม่ใช่ jump เดียว — เคสนี้
         น่าสงสัยน้อยกว่ากลุ่มบน อาจเป็นธุรกิจถดถอยจริง ไม่ใช่ artifact)
  INSURE: 67.7 → 19.5 → 17.8 → **1.86** (ลดต่อเนื่อง 3 ปีติด คล้าย SPCG — ต้องดูเพิ่มว่า
         DPS ปีนี้เป็นปันผลพิเศษ/ค้างจ่ายจากปีก่อนหรือไม่ ก่อนตัดสินว่าเป็นบั๊ก)

โฟกัสที่ (silent calc error โดยเฉพาะ):
- เพิ่ม guard เทียบ eps_value ปีล่าสุดกับ median ของปีอื่นที่มีอยู่ใน payload เดียวกัน
  (ใช้ pattern เดียวกับ `other_cogs`/scale_ref ที่แก้ไปแล้วใน compute_cash_cycle —
  sources/financials_store.py:4384-4391) — threshold ต้องคิดแยกจาก COGS เพราะหน่วยเป็น
  บาท/หุ้นคนละสเกล (ต้องดูข้อมูลจริงเพิ่มว่า 20% เหมือนเดิมเหมาะไหม หรือควรหลวมกว่านั้น
  เพราะ EPS สวิงปีต่อปีเป็นเรื่องปกติมากกว่า COGS)
- ระวังเคสที่ EPS ลดลง "ต่อเนื่องหลายปี" (SPCG/INSURE) ต่างจากเคส "ทรุดฮวบปีเดียว"
  (CHASE/NAT/HENG/K) — กลุ่มแรกอาจเป็นธุรกิจถดถอยจริงที่ payout ratio สูงมีความหมายจริง
  (จ่ายปันผลคงที่ทั้งที่กำไรหด) ไม่ควร null ทิ้งแบบเดียวกับกลุ่มหลัง ต้องแยกให้ออกก่อนแก้
- เช็คว่า DPS ปีนั้น (`dps_year`) มีปันผลพิเศษ/ปันผลค้างจ่ายจากปีก่อนปนมาหรือไม่ (จะทำให้
  payout ratio สูงจริงแม้ EPS ปกติ ไม่ใช่บั๊กจากฝั่ง EPS อย่างเดียว)

>>> แก้แล้ว 2026-09-06 (/code-review medium ยิงจริง เจอ 3 finding, CONFIRMED ทั้งหมด):
  1. sources/factor_snapshot.py:_latest_eps — เพิ่ม guard เทียบ EPS งวดล่าสุดกับ median ของ
     ปีอื่นใน `row` เดียวกัน (ข้อมูลมีอยู่แล้วในมือตรงจุดนี้พอดี ไม่ต้อง plumb ข้อมูลใหม่ —
     `row` คือ dict ปีทั้งหมดของ "Diluted EPS"/"Basic EPS" ก่อนจะเลือก `max(row.keys())`)
     threshold ใช้ 15% (หลวมกว่า COGS 20% ตามที่ตั้งข้อสังเกตไว้) — คำนวณจริงจากเคสยืนยัน
     แล้วว่าแยกกลุ่มได้ถูกจุด: CHASE/NAT/HENG/INSURE/AIE/CEYE/LEO/LPN/SMD100 (ratio ต่อ
     median 0-15%) ถูก null ทิ้ง แต่ SPCG (20.7%) และ K (33%) ไม่ถูกแตะ — ตรงกับที่ตั้ง
     ข้อสงสัยไว้ว่า SPCG อาจเป็นธุรกิจถดถอยจริงไม่ใช่ artifact เพราะ EPS ปีอื่นของ SPCG เอง
     ก็ไม่ได้นิ่ง (declining ต่อเนื่อง 4 ปี ไม่ใช่ jump เดียว) ทำให้ median เองก็ต่ำลงตาม —
     effect เดียวกันแก้ทั้ง payout_ratio_pct (dividend_stats.py) และจุดอื่นที่ใช้
     eps_latest จาก snapshot เดียวกัน (Tearsheet app.py:6893, pbv_pe_screener.py:192
     Justified P/E) ไปด้วยในตัว เพราะ null ที่ต้นทาง _latest_eps เลย
  2. sources/dividend_stats.py:yoy_growth_pct + _cagr — เพิ่ม helper `_near_zero_base()`
     (pattern เดียวกัน, threshold 15%) เทียบปีฐาน (prev ของ yoy / first ของ cagr) กับ
     median ของ DPS ปีอื่นในประวัติเดียวกัน (ข้อมูลมีอยู่แล้วใน `annual` ไม่ต้อง query
     เพิ่ม) — 2 finding นี้ auditor เจอเพิ่มเองนอกเหนือจากที่ระบุไว้ในไฟล์นี้ (compute
     payout ratio) แต่เป็น failure mode เดียวกันเป๊ะเลยแก้พร้อมกัน
  3. ไม่แก้ dps_year (จุดที่ 3 ในโฟกัสข้างบน — เช็คปันผลพิเศษ/ค้างจ่าย): ยืนยันจากโค้ดแล้วว่า
     ไม่ใช่ silent calc error ที่แก้ได้ด้วย guard เชิงตัวเลข (ต้องรู้ context ทางบัญชีที่ไม่มี
     field ระบุในข้อมูลที่ดึงมา) — บันทึกไว้เป็น data-quality caveat ไม่ใช่บั๊ก
  ทดสอบจริง: เทียบ /api/dividends/TH/<sym> ก่อน/หลัง rebuild factor_snapshot (local recompute
  จาก financials.db ที่ sync ไว้แล้ว ไม่ยิง network — 913 TH + 774 DR, ~65 วิ) — payout ratio
  >200% ทั้งตลาดลดจาก 24/548 (4.4%) เหลือ 12/530 (2.3%) กลุ่มที่เหลือส่วนใหญ่สมเหตุสมผล
  (GVREIT/GAHREIT/TU-PF เป็น REIT/trust ที่จ่ายจาก distributable income ไม่ใช่ EPS ตามธรรมชาติ
  อยู่แล้ว, TASCO eps=0.72 ไม่ใช่ค่าจิ๋ว)
  restart server ผ่าน /api/restart (safe restart, pre-flight compile check) แล้วยิง API จริง
  ยืนยันก่อน/หลังทั้ง 2 รอบ (ก่อน rebuild snapshot ยังเห็นค่าเก่าเพราะ cache ตาราง — เป็น
  behavior เดิมของระบบ ไม่ใช่บั๊กใหม่ที่แก้นี้สร้างขึ้น, rebuild เสร็จแล้วค่าถูกต้องทันที)

>>> แก้เพิ่ม 2026-09-06 (ตามหลัง — พบระหว่างทดสอบข้างบน ไม่ใช่ finding เดิมของ /code-review
  ที่ยิงไปตอนแรก): yoy_growth_pct เทียบ "2 ปีล่าสุดที่มีข้อมูล" (`complete_years[-1]`/`[-2]`)
  โดยไม่เช็คว่าติดกันจริงไหม — หุ้นที่หยุดจ่ายปันผลไปหลายปีแล้วกลับมาจ่ายใหม่ (เช่น INSURE:
  2008 → ว่าง 16 ปี → 2025) จะได้ growth ข้ามหลายปีแต่โชว์เป็น "YoY" หลอก (INSURE เคยได้
  900% ทั้งที่จริงคือ growth สะสม 17 ปี) จุด `streak_paid`/`streak_nondecreasing` ในไฟล์
  เดียวกันมี gap-guard นี้อยู่แล้ว (`if y != complete_years[i+1]-1: break`) แต่ yoy ไม่มี —
  เพิ่ม guard `if y0 == y1 + 1` ก่อนคำนวณ (sources/dividend_stats.py ส่วน yoy) ไม่แตะ
  cagr_5y/10y_pct เพราะ annualize ด้วย `span = last_y - first_y` เองอยู่แล้ว ถูกต้องแม้มี
  ช่องว่างปี
  ทดสอบจริง: scan ทั้งตลาด 1515 หุ้นที่มีประวัติปันผล พบ 78 ตัวที่ 2 ปีล่าสุดไม่ติดกัน (ไม่ใช่
  edge case หายาก) ทุกตัว yoy กลายเป็น None ถูกต้องหลังแก้ (จาก 807 เหลือ 729 ตัวที่ยัง
  compute ได้) ยืนยันผ่าน /api/dividends/TH/<sym> จริงหลัง restart server: INSURE/TRUE/KASET
  (gap) → None ถูกต้อง, PTT/CPALL (ปีติดกันปกติ) → ค่าเดิมไม่เปลี่ยน ไม่กระทบ


########################################################################
[2] เมนู: ⚖️ Valuation Band — percentile เทียบกับประวัติตัวเอง
สถานะ: ✅
ความเสี่ยงประเมิน: ต่ำ (อ่านโค้ดคร่าวๆ แล้วพบว่า hardened ไว้ค่อนข้างดีแล้ว)
########################################################################

/code-review medium sources/financials_store.py

ไฟล์/บรรทัด: sources/financials_store.py:5012-5100+ (compute_valuation_percentile,
_drop_spikes, _drop_stuck_tail, _stale)

หมายเหตุ (อ่านโค้ดคร่าวๆ ระหว่างร่างไฟล์นี้แล้ว — ยังไม่ query ข้อมูลจริง): ฟังก์ชันนี้ผ่าน
การ harden มาแล้วจริงจากบั๊กที่เคยเจอจริง มี comment อ้างอิงเคสจริงที่เคยแก้ 2 จุด:
  1. `_drop_spikes` (mult=20, สัมพัทธ์กับ median ของ series ตัวเอง ไม่ใช่ absolute cap) —
     comment อ้าง ADVANC EV/EBITDA นิ่ง 8-10x มา 65 ไตรมาส กระโดดเป็น ~1043x งวดเดียว
     (เคยเจอจริง แก้ไปแล้ว)
  2. `_drop_stuck_tail` (min_repeat=4) — comment อ้าง KASET EV/EBITDA ค่า 567.7013 ซ้ำเป๊ะ
     7 ไตรมาสติด (provider ไม่ได้คำนวณใหม่ ไม่ใช่ธุรกิจนิ่งจริง) — เคยเจอจริง แก้ไปแล้ว
  3. `_stale` (>190 วัน) กันหุ้นพลิกขาดทุน/งดปันผลค้างค่าเก่าจากหลายปีก่อน
  4. `min_points=12` กันหุ้นข้อมูลน้อยเกินไปไม่มีนัยยะ

โฟกัสที่ (ยังไม่ได้ยืนยันด้วยข้อมูลจริง — ถ้าจะทำต่อควรเริ่มจากจุดนี้):
- query จริงหา symbol ที่ series ผ่าน `_drop_spikes`/`_drop_stuck_tail` แล้วยัง<12 จุดเหลือ
  พอดี (border case ของ min_points) ดูว่า percentile ที่ได้ยังน่าเชื่อถือไหม
- เช็คว่า `_drop_spikes` เองก็เสี่ยง near-zero-base ไหม (med<=0 return pts ไม่กรอง — ถ้า
  median ของ series ติดลบทั้งหมด ฟังก์ชันจะข้าม filter ไปเลย เป็นไปได้ไหมกับ PE/PBV/EV
  ที่กรอง `v is not None and v > 0` มาก่อนแล้วที่ต้นทาง — น่าจะเป็นไปไม่ได้จริงเพราะจุดที่
  เข้า _drop_spikes ล้วนเป็นค่าบวกอยู่แล้ว แต่ควรยืนยันด้วยโค้ดอีกรอบ)

>>> ตรวจแล้ว/แก้แล้ว 2026-09-06 — /code-review medium เจอ 3 จุด ยืนยันด้วยข้อมูลจริงจาก
financials.db ครบทั้ง 3 (ไม่ใช่แค่อ่านโค้ด) แก้ 2/3:

  [แก้แล้ว] div_yield ข้าม _drop_stuck_tail ทั้งที่ควรใช้ (เดิม code กัน div_yield ออกจาก
  ทั้ง _drop_stuck_tail และ _drop_spikes พร้อมกัน ทั้งที่ comment justify ไว้แค่ฝั่ง
  _drop_spikes เท่านั้น) — สแกน finnomena_q จริง 1,202 symbol เจอ 7 ตัวที่ div_yield ค้างค่า
  ซ้ำเป๊ะ >=4 ไตรมาสติด (A/BTSGIF/CIMBT/DR:NVDA/DTAC/SA/TSR) เทียบก่อน/หลังแก้ตรงๆ:
    CIMBT OLD value=10.81 percentile=89.2 (ของจริงคือ provider ค้างค่าตั้งแต่ก่อนงวดล่าสุด)
          NEW value=None stale=True  ← ถูกต้อง
    TSR   OLD value=0.32 percentile=8.3 n=12 (พอดี min_points เพราะนับจุดค้างรวมด้วย)
          NEW n เหลือ 1 จริง → value=None stale=True
    DTAC  OLD value=4.67 (ทั้ง series 5 จุดเป็นค่าเดียวกันหมด แต่โชว์เป็น "ปัจจุบัน")
          NEW n=0 → value=None
  ยืนยันผ่าน API จริงด้วย: rebuild factor_snapshot (local recompute จาก financials.db,
  ไม่ยิง network, ~87 วิ) แล้วยิง /api/factor-screener?symbol=CIMBT ก่อน/หลัง — div_yield
  10.81 → null ตรงกับที่คำนวณ, ยิงซ้ำ TSR/SA/A ก็ null ตรงกันหมด, จำนวนแถวรวม 1687 ไม่พัง
  แก้: sources/financials_store.py — ย้าย `pts = _drop_stuck_tail(pts)` ออกมานอก if-guard
  ให้รันทุก metric รวม div_yield, เหลือแค่ _drop_spikes ที่ skip เฉพาะ div_yield

  [แก้แล้ว] ref_date fallback เมื่อไม่มี Close เลย เดิม derive จาก max date ของ metric
  เดียวกับที่กำลังเช็ค stale เอง (self-referential) ทำให้ _stale() เทียบกับตัวเองเสมอ (gap
  เป็น 0 ตลอดกาล ไม่ว่าข้อมูลจะเก่าแค่ไหนจริงๆ) — สแกนจริงพบ 5,436/33,768 symbol (ส่วนใหญ่
  FINN:HK/FINN:US ตัวเล็ก + ไทยไม่กี่ตัวเช่น TFFIF/VAYU1/TRUEE) ไม่มี Close เลย ในนั้น 13 ตัว
  มี metric อื่นให้ ref_date เพี้ยนจริง (เช่น FINN:US:COWN ref_old=2023-03-31 กลายเป็น
  "ปัจจุบัน" ทั้งที่วันจริงคือ 2026-09-06) แก้: ref_date fallback เป็น
  `_date.today().isoformat()` แทนการ derive จาก val เอง

  [ไม่แก้ — confirmed dead code] `_drop_spikes` บรรทัด med<=0 return pts ยืนยันแล้วว่า
  unreachable จริงตามที่สงสัยไว้ — จุดที่เรียก _drop_spikes ทั้งหมด (pe/pbv/ev_ebitda) กรอง
  v>0 มาก่อนแล้วทุกทาง ไม่แตะเพราะไม่ใช่บั๊ก แค่ defensive branch เกินจำเป็น

restart server ผ่าน /api/restart (safe restart) แล้ว rebuild factor_snapshot ยืนยันผ่าน
/api/factor-screener จริงทั้งก่อน/หลัง (ดูรายละเอียดด้านบน)


########################################################################
[3] เมนู: 📈 Tearsheet — DCF Interactive Model (Reverse/Forward/Sensitivity)
สถานะ: ✅
ความเสี่ยงประเมิน: ต่ำ (อ่านโค้ดคร่าวๆ แล้วพบ guard r<=terminalGrowth ครบทุกจุดเรียกแล้ว)
########################################################################

/code-review medium static/dashboard.js

ไฟล์/บรรทัด: dashboard.js:13091-13260 (_tsDcfHtml/_tsDcfModelHtml), 13545-13574
(_dcfPvOfStream/_dcfImpliedGrowth/_dcfForwardFairValuePerShare), 13576-13660+
(_tsDcfRecalc, sensitivity table)

หมายเหตุ (อ่านโค้ดคร่าวๆ ระหว่างร่างไฟล์นี้แล้ว — ไล่ทุก call site ของ _dcfPvOfStream):
กังวลเดิมคือ Terminal Value = `cf*(1+tg)/(r-tg)` (Gordon Growth) ระเบิดถ้า r≤tg (pattern
เดียวกับ CALC_RISK_AUDIT รอบ 1 ข้อ [6] "r ใกล้ g" ของ Fair Value P/B·P/E) — ไล่ดูจริงแล้ว
พบว่า **มี guard ครบทุกจุดที่เรียกจริง**:
  - `_dcfImpliedGrowth` (Reverse DCF): `if (fcf0 <= 0 || r <= terminalGrowth) return null;`
    (บรรทัด 13557)
  - `_dcfForwardFairValuePerShare` (Forward DCF + Sensitivity table ทั้งคู่เรียกผ่านนี้):
    `if (r <= terminalGrowth || !shares) return null;` (บรรทัด 13570)
  - Sensitivity table axis เป็น free-text (ผู้ใช้พิมพ์ discount rate เองได้ไม่จำกัดช่วง
    ต่างจาก input หลักที่ HTML บังคับ min=6/max=15) แต่ก็ยังผ่าน
    `_dcfForwardFairValuePerShare` ตัวเดียวกันทุก cell — guard ครอบอยู่แล้ว ไม่มีทางหลุด
  - `_dcfPvOfStream` เอง (ฟังก์ชันแกนกลางที่ไม่มี guard ในตัว) ไม่เคยถูกเรียกตรงจากที่ไหน
    เลยนอกจาก 2 wrapper ข้างบนที่ guard ไว้แล้วก่อนเรียกเสมอ

โฟกัสที่ (ถ้าจะทำต่อ — ความเสี่ยงต่ำแล้วแต่ยังไม่ query ข้อมูลจริง):
- ยืนยันด้วย Playwright จริงว่า UI แสดงข้อความ "คำนวณไม่ได้ด้วยพารามิเตอร์นี้" ที่ถูกต้อง
  เมื่อผู้ใช้พิมพ์ r<=tg จริงในช่อง sensitivity (ยังไม่เคยเปิด browser ทดสอบ เป็นแค่การ
  อ่านโค้ด)
- เช็คว่า `_dcfImpliedGrowth` bisection (บรรทัด 13556-13567, ช่วงค้นหา g ใน [-0.3, 0.6])
  มีเคสที่ EV ติดลบ/ผิดปกติ (mkt_cap < netCash มาก) ทำให้ bisection ไม่ converge/ได้ค่า
  หลอกหรือไม่ — ยังไม่ได้ตรวจจุดนี้

>>> รีวิว /code-review medium ตรวจแล้ว/แก้แล้ว 2026-09-06
subagent เจอ 2 finding จริง (สิ่งที่โน้ตข้างบนพลาด): guard เดิม `r <= terminalGrowth` เช็ค
แค่ "เครื่องหมาย" ไม่เช็ค "ระยะห่างขั้นต่ำ" — ถ้า r เฉียด tg (เช่น r=4.01%, tg=4%) ผ่าน guard
ได้แต่ Terminal Value หารด้วย 0.0001 พุ่งเป็นพันเท่าแบบเงียบๆ ไม่มี warning:
  [1] _dcfImpliedGrowth (13557) + _dcfForwardFairValuePerShare (13570, ที่ Forward DCF +
      Sensitivity table ทั้งคู่เรียกผ่านนี้) — guard เดียวกัน เจอจริง
  [2] DCF Model (_tsDcfModelRecalc, `wacc <= tg` บรรทัด 13391, สูตร TV บรรทัด 13450) — guard
      แยกก็อปมาคนละจุด ไม่ผ่าน wrapper ข้างบนเลย (เขียนสูตร TV เองตรงๆ ในฟังก์ชันนี้) พังแบบ
      เดียวกันได้อิสระจากข้อ [1]

แก้: เพิ่มค่าคงที่ร่วม `DCF_MIN_TV_GAP = 0.005` (0.5 percentage point) ก่อน `_dcfPvOfStream`
(บรรทัด ~13545) แล้วเปลี่ยนทั้ง 3 จุดเช็คจาก `r <= tg` เป็น `r - tg < DCF_MIN_TV_GAP` —
[1] `_dcfImpliedGrowth` [2] `_dcfForwardFairValuePerShare` [3] `_tsDcfModelRecalc` (wacc vs tg)
+ อัปเดตข้อความ error ทั้ง 2 จุด (reverse DCF + DCF Model) ให้บอกระยะห่างขั้นต่ำที่ต้องการ
ไม่ใช่แค่ "ต้องมากกว่า" เฉยๆ · bump `?v=20260906e`

ทดสอบจริงผ่าน Playwright (เปิด server จริงที่ :5001, evaluate ฟังก์ชันตรงในบราวเซอร์ ไม่ใช่
อ่านโค้ดอย่างเดียว): r=4.01%/tg=4% (ห่าง 0.01pt) → null ทั้ง Forward และ Reverse ✅,
r=10%/tg=2% (ห่างปกติ) → ได้ค่าปกติไม่ null ✅, console 0 error ตอนโหลดหน้า ✅ — ยังไม่ได้
ทดสอบ DCF Model guard ([2]) ผ่าน UI จริง (ต้องเปิด tearsheet คลิกไปแท็บ DCF Model ซึ่งซับซ้อน
กว่า evaluate ตรง) แต่ใช้ constant+เงื่อนไขเดียวกันกับ [1] ที่ยืนยันแล้ว ความเสี่ยงเหลือต่ำ
ไม่แตะ `_dcfPvOfStream` core formula เอง (ยังไม่มี guard ในตัว ตามที่โน้ตเดิมสรุปถูกแล้วว่า
ไม่เคยถูกเรียกตรง)


########################################################################
[4] เมนู: 🎯 ฉันทามตินักวิเคราะห์ (Analyst Consensus) — target price/rec_mean
สถานะ: ✅
ความเสี่ยงประเมิน: ต่ำมาก (เป็น relay ข้อมูลจาก yfinance ตรงๆ ไม่ได้คำนวณหารเองในจุดเสี่ยง)
########################################################################

/code-review low sources/analyst_consensus.py

ไฟล์/บรรทัด: sources/analyst_consensus.py:143-149 (_rec_mean), 225-300 (fetch_one —
target_mean/high/low/median)

หมายเหตุ (อ่านโค้ดคร่าวๆ ระหว่างร่างไฟล์นี้แล้ว): ต่างจาก [1]/[2]/[3] เพราะจุดนี้**ไม่มี
การหารด้วยฐานที่มาจากข้อมูลของเราเอง** — target_mean/high/low/median (บรรทัด 284-287)
ดึงจาก `t.analyst_price_targets` ของ yfinance ตรงๆ (yfinance คำนวณ aggregate มาให้แล้ว
ไม่ใช่โค้ดเราเอง sum/average เอง) ส่วน `_rec_mean` (weighted average ของคะแนน 1-5 ถ่วง
น้ำหนักจำนวนโบรกแต่ละระดับ) มี guard `if not total: return None` (บรรทัด 146) และผลลัพธ์
ถูก bound ทางคณิตศาสตร์อยู่แล้วในช่วง [1,5] เสมอ (ค่าถ่วงน้ำหนักคงที่ ไม่มีทางระเบิดได้
ต่างจากอัตราส่วนการเงินที่ตัวหารแปรผันได้)

โฟกัสที่ (ถ้าจะทำต่อ — ความเสี่ยงต่ำมากแล้ว อาจไม่คุ้มเวลา):
- เช็คจุดที่ dashboard.js เอา target_mean ไปคำนวณ upside % ต่อ (target_mean/price_current
  -1)*100 — ถ้ามี ต้องดูว่า price_current ใกล้ 0 ได้ไหม (หุ้น penny stock) แต่คาดว่าความ
  เสี่ยงต่ำเพราะราคาหุ้นแทบไม่มีทางเป็น 0/ติดลบ ต่างจาก revenue/equity ที่ทำได้จริง

>>> ตรวจแล้ว 2026-09-06: /code-review low ไม่มี diff แตะไฟล์นี้เลย (diff เดียวในสาขาคือจุด
DCF ของ [3]) เลยไปเช็ค "โฟกัสที่" ต่อเอง — dashboard.js:12377 `up = (a.target_mean != null &&
price) ? (a.target_mean / price - 1) * 100 : null` มี guard `&& price` กัน 0/null/undefined/
NaN อยู่แล้ว ราคาหุ้นจริงก็ไม่มีทางติดลบ ไม่มีบั๊ก · จุด _tsDcfBaseFairValue (บรรทัด 12399)
ก็มี guard `> 0` อยู่แล้วเช่นกัน · ไม่มีโค้ดต้องแก้ ไม่ต้อง restart/bump ?v=


########################################################################
[5] เมนู: 📈 แนวโน้มตลาด (Market Trend) — roe_aggregate/cfo_np_aggregate/_sum_yoy
สถานะ: ✅
ความเสี่ยงประเมิน: ต่ำ (อ่านโค้ดคร่าวๆ แล้วพบว่ามี guard ป้องกัน pattern นี้ไว้แล้วชัดเจน
พร้อม comment อ้างอิงบั๊กเดิมที่เคยแก้)
########################################################################

/code-review medium sources/financials_store.py

ไฟล์/บรรทัด: sources/financials_store.py:3659-3850+ (get_market_trend), โดยเฉพาะ
_sum_yoy (3741-3748), roe_aggregate (3786-3795), cfo_np_aggregate (3796-3802) — ใช้ร่วม
กันทั้ง /api/market-trend (5491) และ /api/sector-trend (5529, สโคปแคบกว่า เสี่ยงกว่าตาม
comment ในโค้ดเอง)

หมายเหตุ (อ่านโค้ดคร่าวๆ ระหว่างร่างไฟล์นี้แล้ว): ทั้ง 3 จุด aggregate มี guard ตรงกับ
pattern ที่ CALC_RISK_AUDIT ตามหาอยู่แล้วครบ พร้อม comment ยืนยันว่าเป็นการแก้บั๊กที่เคย
เจอจริงมาก่อน (บั๊กเดียวกับที่ round 1 ข้อ [4] "เปรียบเทียบ Sector" เจอ):
  - `_sum_yoy`: `ps > 0` (บรรทัด 3748, comment "ps > 0 ไม่ใช่แค่ != 0")
  - `roe_aggregate`: กรองเฉพาะ `eq and eq > 0` ก่อนเข้า pool (บรรทัด 3786) ไม่ปล่อย
    equity ติดลบเข้าไปเลยตั้งแต่ต้น (ต่างจาก [ROUND3][2] ที่เพิ่งแก้ ที่โค้ดเดิมปล่อยผ่าน
    equity ติดลบเข้าสูตรตรงๆ — จุดนี้ปลอดภัยกว่าตั้งแต่แรก)
  - `cfo_np_aggregate`: `cfo_np_denom > 0` (บรรทัด 3802) พร้อม comment อธิบายตรงประเด็น
    "หาร CFO ด้วยกำไรรวมติดลบพลิกเครื่องหมาย ได้ ratio หลอก" — ระบุชัดว่าเคยคิดเผื่อ
    /api/sector-trend (หุ้นน้อยตัว เสี่ยงกว่า) ไว้แล้วตั้งแต่ตอนเขียน

โฟกัสที่ (ถ้าจะทำต่อ — ความเสี่ยงต่ำแล้ว เป็นการยืนยันมากกว่าหาบั๊กใหม่):
- query จริงหา sector เล็กที่ผ่าน /api/sector-trend แล้ว sum equity/cfo_np_denom เป็นบวก
  แต่ "จิ๋วมาก" (ไม่ใช่ 0 เป๊ะ) จากการหักล้างกันเกือบพอดีข้าม 3-5 หุ้นในกลุ่ม — เป็นไปได้
  ยากกว่าเคสหุ้นเดี่ยวมาก (ต้องมีหุ้นในกลุ่มขาดทุน/กำไรพอดีหักล้างกันเกือบสนิท) แต่ยังไม่
  ตัดทิ้งเป็น 0% จนกว่าจะ query ยืนยัน


========================================================================
 หมายเหตุการใช้ไฟล์นี้
========================================================================
- ลำดับความสำคัญที่แนะนำ (ต่างจากลำดับที่ผู้ใช้พิมพ์มา เพราะผลตรวจคร่าวๆ ต่างกันมาก):
  [1] Payout Ratio (มีบั๊กจริงรอแก้) → [5] Market Trend (verify) → [2] Val. Band (verify)
  → [3] Tearsheet DCF (verify) → [4] Analyst Consensus (แทบไม่คุ้มเวลา)
- [2]-[5] ต่างจาก [1] ตรงที่ยังไม่ได้ query ข้อมูลจริงยืนยัน 100% (แค่อ่านโค้ด) — ผลตรวจ
  อาจเปลี่ยนได้ถ้า query จริงแล้วเจอ edge case ที่คาดไม่ถึง แต่จากโครงสร้าง guard ที่มีอยู่
  ความเสี่ยงต่ำกว่า [1] ค่อนข้างชัดเจน
- ใช้ effort "medium" ส่วน [4] ใช้ "low" พอ (ขอบเขตเล็ก ความเสี่ยงต่ำมาก)
- กติกาสถานะเหมือนไฟล์อื่น: ⬜ ยังไม่รีวิว → 🔶 รีวิว/แก้แล้วรอผู้ใช้ยืนยัน → ✅ ยืนยันแล้ว
  (Claude ห้ามแก้เครื่องหมายสถานะเอง ให้ผู้ใช้ติ๊กเองเสมอ)
- ไฟล์นี้เป็น snapshot วันที่สร้าง (2026-09-06) — ถ้าโค้ดเปลี่ยนไปมากควรไล่ตรวจโค้ดจริง
  ใหม่ก่อนเชื่อเลขบรรทัด/หมายเหตุในนี้
========================================================================
