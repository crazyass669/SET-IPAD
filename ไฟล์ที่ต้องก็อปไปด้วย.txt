====================================================
  SET Dashboard — ไฟล์ที่ต้องก็อปไปด้วยเวลาย้ายเครื่อง
  (อัปเดต 2026-07-15 — หลังเพิ่ม Backtest suite, positive-streak filters,
   คำอธิบายบริษัทหุ้นไทย, แถบ PE/PBV ใน popup + จัดระเบียบไฟล์ไม่ได้ใช้)
====================================================

[ จำเป็นต้องมี — โค้ด ก็อปทุกไฟล์/โฟลเดอร์นี้ ]

  start.bat                    ← ดับเบิ้ลคลิกเปิดโปรแกรม
  app.py                       ← Flask Web Server (หัวใจหลัก)
  set_data_fetcher.py          ← โค้ดดาวน์โหลดข้อมูลจาก Yahoo Finance
  set_dashboard.html           ← โครง HTML ของหน้าเว็บ
  requirements.txt             ← รายการ Library ที่ต้องติดตั้ง
  listedCompanies_en_US.xls    ← รายชื่อหุ้น SET (ดาวน์โหลดจาก set.or.th — นามสกุล .xls
                                  จริงๆ ไม่ใช่ .xlsx ดึงตรงจาก set.or.th แบบนี้เสมอ)

  core/                        ← metrics.py (สูตรคำนวณ RS/EMA), store.py (persistence)
  sources/                     ← yahoo.py, tradingview.py, sec.py, dr_universe.py,
                                  financials_store.py, factor_snapshot.py, dr_descriptions.py
                                  (HTTP clients + งบการเงิน/description)
  services/                    ← refresh.py (orchestration), breadth.py (market breadth)
  static/                      ← dashboard.css, dashboard.js (แยกออกจาก HTML)

----------------------------------------------------

[ ข้อมูลที่ดาวน์โหลดแล้ว — ก็อปด้วยถ้าไม่อยากรอ Full Refresh ใหม่ (~1-2 ชม.) ]

  set_prices.db          ← ราคาย้อนหลังทุกตัว (SQLite) — แหล่งหลักของ Breadth/Market
                            Internals/สถิติระยะยาว/Backtest ~260 MB
  set_history.json       ← ประวัติราคาย้อนหลัง (ใช้กราฟ 5Y/Max + EMA200) ~95 MB
  set_data.json          ← ข้อมูลหุ้นทุกตัว (ราคา, RS, EMA, Fundamentals) ~13 MB
  set_data_backup.json   ← backup อัตโนมัติของ set_data.json — app.py fallback มาอ่านไฟล์นี้
                            เองถ้าไฟล์หลักเสียหาย (ดู app.py: BACKUP_FILE) ไม่บังคับต้องก็อป
                            (จะสร้างใหม่เองรอบถัดไปที่ระบบเซฟข้อมูล) แต่ถ้าก็อปไปด้วยจะกัน
                            เคส "ไฟล์หลักพังทันทีที่เปิดเครื่องใหม่แล้วยังไม่มี backup"
  dr_cache.json          ← ข้อมูล DR/DRx ต่างประเทศ ~35 MB
  indices_cache.json     ← ข้อมูลดัชนีกลุ่ม 30+ ดัชนี ~5 MB
  nvdr_data.json         ← ข้อมูล NVDR (ต่างชาติถือครอง)
  short_sales_data.json  ← ข้อมูล Short Sales
  set_market_stats.json  ← สถิติ PE/PBV ตลาดรวม
  market_flow_data.json  ← ข้อมูล flow signals (Confluence page)
  rotation_state.json    ← state ของ Rotation alert (กลุ่มไหนแจ้งเตือนไปแล้ว)
  Table_PE.xls           ← ข้อมูล valuation อ้างอิงราย sector
  Table_PBV.xls          ← ข้อมูล valuation อ้างอิงราย sector

  ** ถ้าไม่ก็อปไฟล์ข้อมูล → ต้อง Full Refresh ใหม่ในเครื่องใหม่ (~1-2 ชั่วโมง)

----------------------------------------------------

[ ข้อมูลงบการเงิน — local-only ห้าม push ขึ้น GitHub (ดู .gitignore) แต่ก็อปย้ายเครื่องได้ปกติ ]

  financials.db          ← งบการเงินเต็ม (Yahoo/Finnomena/SET) + factor_snapshot ที่ Screener+
                            ใช้ ~630 MB (ใหญ่สุดในโปรเจกต์ — ถ้าไม่ก็อป Screener+/หน้างบการเงิน
                            จะว่างเปล่า ต้องอัพเดทงบการเงินใหม่ทั้งหมดในเครื่องใหม่)
  sec_filings.db         ← SEC filings สะสม (แบบ 59/246-2) สำหรับหน้า Insider ~2.5 MB
  dr_descriptions.json   ← คำอธิบายบริษัท (EN+แปลไทย) หุ้นไทย+DR ที่ sync ไว้แล้ว ~4 MB
                            (มาจาก Yahoo longBusinessSummary — อนุญาต push GitHub ได้ตามกฎเดิม
                            ต่างจาก financials.db/sec_filings.db ที่ห้าม)

----------------------------------------------------

[ คู่มือ / เอกสารอ้างอิง — ก็อปไว้อ่านที่เครื่องใหม่ (ไม่จำเป็นสำหรับการรันโปรแกรม) ]

  คู่มือการใช้งาน.html         ← เปิดด้วย Browser อ่านได้เลย ไม่ต้องรัน Server
  คู่มือติดตั้ง.md             ← วิธีติดตั้งตั้งแต่ต้น (Markdown)
  BACKTEST_SUMMARY.txt        ← สรุปผล backtest 19 กลยุทธ์ 6 หมวด (2026-07-15)
  PLAN_screener_financials.txt← แผนงาน Screener+/งบการเงิน (มี log ผลทดสอบที่ทำไปแล้ว)
  PROMPT_fable_us_hk_preset.txt← โน้ตงานเกี่ยวกับ preset หุ้น US/HK
  SESSION_SUMMARY_2026-07-15.txt ← สรุปงานเซสชันล่าสุด

----------------------------------------------------

[ Backtest research scripts — ไม่จำเป็นต้องรันแอปหลัก แต่เก็บไว้ทดสอบกลยุทธ์เพิ่มได้ ]

  bt_lib.py                        ← เครื่องมือกลาง (โหลดข้อมูล/indicator/engine เดินพอร์ต)
  backtest_rs_rrg.py                ← strategy หลักเดิม (RS Score + RRG)
  analyze_ohlc_backtest.py          ← enhancement (ATR sizing, near-52wk-high, stop-loss)
  backtest_trend_following.py       ← SMA/EMA Cross, MACD, ADX, SuperTrend, Ichimoku
  backtest_mean_reversion.py        ← RSI, Close<SMA20-10%
  backtest_bollinger_stochastic.py  ← Bollinger Bands, Stochastic
  backtest_breakout.py              ← Donchian Breakout, ATR Breakout
  backtest_pure_momentum.py         ← 12-Month Momentum เดี่ยว
  backtest_seasonal.py              ← Sell in May, January Effect
  backtest_money_management.py      ← DCA vs Grid, Pyramiding proxy
  backtest_screener.py              ← Backtest เงื่อนไข Screener+ ย้อนหลัง (fundamentals)
  backtest_report.html              ← หน้ารายงานผล (เปิดผ่าน app.py serve)

----------------------------------------------------

[ ไม่ต้องก็อป — สร้างเองอัตโนมัติ / เป็นของขยะ ]

  __pycache__/             ← Python cache (สร้างใหม่เองตอนรัน)
  logs/                    ← log ของ waitress (สร้างใหม่เอง)
  ไฟล์ไม่ได้ใช้/            ← โฟลเดอร์รวมไฟล์ที่ตรวจสอบแล้วว่าไม่มีโค้ดไหนเรียกใช้เลย
                             (สคริปต์ one-time ที่ทำเสร็จแล้ว, log ชั่วคราว, ไฟล์ test/backup
                             เก่า) เก็บไว้เผื่ออยากดูย้อนหลัง แต่ไม่ต้องก็อปไปเครื่องใหม่ก็ได้

  ** สคริปต์ one-time ที่ยังเก็บไว้นอกโฟลเดอร์ (เผื่อใช้ก็ได้ แต่ไม่จำเป็นต้องรีบก็อป):
     import_eod_archive.py, import_market_stats.py, migrate_history.py,
     build_mirror_names.py, mirror_finnomena.py, check_mirror.py + check_mirror.bat,
     update_financials.py + update_financials.bat, sync_th_descriptions.py

====================================================

[ ขั้นตอนหลังก็อปไปเครื่องใหม่ ]

  1. วางทั้งโฟลเดอร์ไว้ที่ไหนก็ได้ในเครื่องใหม่ (ต้องก็อป core/ sources/ services/ static/
     ไปพร้อมกันด้วย ไม่งั้น app.py จะ import ไม่เจอแล้วรันไม่ขึ้น)
  2. ตรวจสอบว่าติดตั้ง Python 3.10+ แล้ว
     → โหลดที่ python.org (ติ๊ก "Add to PATH" ตอนติดตั้ง)
  3. ดับเบิ้ลคลิก start.bat
     → จะติดตั้ง Library อัตโนมัติครั้งแรก (flask, yfinance, pandas, waitress, deep-translator ฯลฯ)
  4. Browser เปิดขึ้นเองที่ http://localhost:5001
  5. ถ้าไม่ได้ก็อปไฟล์ข้อมูล → กด Full Refresh เพื่อดาวน์โหลดใหม่
  6. ถ้าไม่ได้ก็อป financials.db → หน้า Screener+/งบการเงินจะว่าง ต้องกด
     "🔄 อัพเดทงบการเงินทั้งหมด" ในหน้างบการเงินใหม่ (ใช้เวลานาน — เป็นชั่วโมง)

====================================================
